services:
  ai-summary-app:
    build: .
    container_name: ai-summary-container
    ports:
      - "5000:5000"
    volumes:
      - ./config.json:/app/config.json
      - ./data:/app/data
      - ./logs:/app/logs
      - ./output:/app/output
    restart: unless-stopped
